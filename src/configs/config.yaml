defaults:
  - _self_
  # - model: ijepa
  # - trainer: ijepa
  - model: segmentation
  - trainer: segmentation
  - data/processor: norm

data:
  root_dir: "/mnt/toshiba_hdd/code/robot-navigation/marsls-seg/data/marsls-seg/mmlsv2"
  params_file: "/mnt/toshiba_hdd/code/robot-navigation/marsls-seg/data/configs/mmls_norm.yaml"
  eval_split: test

training:
  batch_size: 32
  lr: 1e-4
  num_workers: 0
  n_epochs: 500
  pin_memory: true
  # Evaluate model every ? epochs
  eval_int: 1
  # Save model ckpt every ? epochs
  save_int: 60
  # Where to save the model?
  ckpt_dir: /mnt/toshiba_hdd/code/robot-navigation/marsls-seg/data/runs/ms-jepa

wandb:
  project: MarsLS-JEPA
  mode: "online"
  artifact_name: "marsls-ijepa"
  model_desc: ""
  group: ???
  run_name: ""
